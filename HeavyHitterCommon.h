#ifndef __heavy_hitter_common_hpp__
#define __heavy_hitter_common_hpp__

#define MIN(x, y) x < y ? x : y
#define MAX(x, y) x > y ? x : y

/*
static int long_long_cmp (const void * a, const void * b) {
    return *(long long*)a - *(long long*)b;
}
*/

#endif
